#include<stdio.h>
int main()
{
    int n, i, a[20];
    for(i=0; i<20; i++)
    {
        scanf("%d", &a[20]);
    }
    for(i=19; i>=0; i--)
    {
        printf("N[%d] = %d\n", i, a[i]);
    }
}
