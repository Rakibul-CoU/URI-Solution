#include<stdio.h>
main()
{
    int n, i, x;
    double int t_C=0, t_R=0, t_S=0, sum=0;
    char ch;
    scanf("%d", &n);
    for(i=1; i<=n; i++)
    {
        scanf("%d%c", &x, &ch);

    }
}
